apiVersion: v1
kind: Service
metadata:
  name: go-web-app
  labels:
    app: go-web-app
spec:
  selector:
    app: go-web-app
  type: ClusterIP
  ports:
  - name: go-web-app
    protocol: TCP
    port: 8080
    targetPort: 8080

